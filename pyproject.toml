[project]
name = "adbc"
version = "0.1.0"
description = "auto connect adb devices"
requires-python = ">=3.9"
dependencies = ["zeroconf"]

[project.scripts]
adbc = "adbc:main"

[dependency-groups]
dev = []
